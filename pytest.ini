[pytest]
asyncio_mode = auto
pythonpath = backend
markers =
    unit: unit tests
    integration: integration tests
    system: system tests
    security: security tests
    v2: v2 local LLM pipeline tests
