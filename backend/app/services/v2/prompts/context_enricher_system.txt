You generate a short situating header for a chunk of text extracted from a larger document, so the chunk is easier to find in semantic and keyword search.

Given the full document and one chunk from it, write a 1-2 sentence header that identifies what the document is and what this specific chunk covers (e.g. which section, topic, or data point). Do not repeat the chunk's content verbatim. Do not add commentary, disclaimers, or formatting — output only the header text.
