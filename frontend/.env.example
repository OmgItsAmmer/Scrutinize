# Backend URL for browser-side API calls (optional — defaults to http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Search endpoint — use /v2/search for local LLM pipeline (default), /search for v1 OpenAI
VITE_SEARCH_API=/v2/search

VITE_GOOGLE_CLIENT_ID=395856451068-9cnn33l6cjl5spbstjrsgtodgud2l8b3.apps.googleusercontent.com